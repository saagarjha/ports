PortSystem          1.0
PortGroup           github 1.0

name                swift-format
description         Formatting technology for Swift source code
long_description    {*}${description}

github.setup        apple swift-format 508.0.0
platforms           darwin

maintainers         @saagarjha

use_configure       no
use_xcode           yes

checksums           rmd160  f4720559fa7c2d02639fcc88f7be7cc24a006a30 \
                    sha256  ccbb17a7c61e39a92ca39246d4e7efc8ef4966a1fd2ffc68631fa7e0f9ee3cc8 \
                    size    225043

build {
	system "cd ${worksrcpath} && swift build --disable-sandbox -c release"
}

destroot {
	xinstall -d ${destroot}${prefix}/bin
	xinstall ${worksrcpath}/.build/release/swift-format ${destroot}${prefix}/bin/swift-format
}
